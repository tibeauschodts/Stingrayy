/**
 * These are the values we want to recieve and show on the application
 */

export interface Asset {
    token_id: string;
    name: string;
    description: string;
    symbol: string;
    created_date: string;
    image_url: string;
    external_link: string;
    opensea_link: string;
}